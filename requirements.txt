django==1.3
# nltk==2.0.1rc1 
# Unfortunately, pip installation of nltk is broken (setup.py at the wrong place) ..
# Use the following command to install it instead:
# pip install -E /path/to/cablegate-env --no-index --find-links=http://nltk.googlecode.com/ nltk
psycopg2
PyYAML
simplejson
django-grappelli
#PIL
#pillow
#Markdown
#easy-thumbnails
#django-debug-toolbar
#django-sentry
